Bindings to tcc/libtcc (tiny c compiler)

Why?
idea was to use a subset of c as ffi (to c) language

Note:
See also the cc module (does the same using an external
compiler/toolchain and the ltdl module)
