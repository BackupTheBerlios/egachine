/*
  tool to convert files to resources
  egachine is not allowed to open files
  => everything must be in one file or received from the server

  resources are base64 encoded
*/

int
main(int argc, char **argv)
{
  char* commonlib="egachine.js";
  int ret=0;
  /*
  if (ECMAScript::init()) {

    // parse config files - this is done before we register our functions
    // since they may use config variables !!

    // now register our objects/functions
    */

  return ret;
}

